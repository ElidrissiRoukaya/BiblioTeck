@{
    ViewData["Title"] = "Accueil";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<style>
    
    body {
        margin: 0;
        padding: 0;
        font-family: 'Segoe UI', sans-serif;
        background: linear-gradient(to bottom right, #fef6f0, #ffd79f);
        height: 100vh;
        overflow: hidden;
    }

    .navbar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 20px 40px;
        background: transparent;
    }

    .logo {
        font-size: 24px;
        font-weight: bold;
        color: #ff8c2e;
    }

    .btn-login {
        background-color: #ff8c2e;
        color: white;
        padding: 10px 20px;
        border: none;
        border-radius: 10px;
        font-size: 16px;
        cursor: pointer;
        transition: background-color 0.3s;
    }

    .btn-login:hover {
        background-color: #e67d24;
    }

    .content {
        display: flex;
        justify-content: space-between;
        align-items: center;
        height: calc(100vh - 80px);
        padding: 0 60px;
    }

    .left-content {
        max-width: 45%;
    }

    .left-content h1 {
        font-size: 48px;
        color: #333;
        margin-bottom: 20px;
    }

    .left-content p {
        font-size: 18px;
        color: #555;
    }

    .right-content img {
        max-width: 500px;
        width: 100%;
        height: auto;
    }

    .modal-content {
        border-radius: 20px;
        padding: 30px;
    }

    .login-title {
        font-size: 28px;
        color: #333333;
        margin-bottom: 20px;
    }

    .login-button {
        background-color: #ff8c2e;
        color: white;
        border: none;
        padding: 12px;
        border-radius: 10px;
        font-size: 20px;
        transition: background-color 0.3s ease;
    }

    .login-button:hover {
        background-color: #e67d24;
    }

    .logo-text {
        font-family: 'Montserrat', sans-serif;
        font-weight: 800;
        font-size: 32px;
        letter-spacing: 2px;
        background: linear-gradient(to right, #ff8c2e, #ff5e62);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        text-shadow: 1px 1px 2px rgba(0,0,0,0.1);
    }

    .highlight {
        font-style: italic;
        font-weight: 900;
    }
</style>

<!-- Navbar -->
<div class="navbar">
    <div class="logo">📚 <span class="logo-text">Biblio<span class="highlight">Net</span></span></div>

    <button class="btn-login" data-bs-toggle="modal" data-bs-target="#loginModal">Login</button>
</div>

<!-- Main content -->
<div class="content">
    <div class="left-content">
        <h1>Bibliothèque en ligne</h1>
        <p>Explorez, empruntez et gérez vos lectures n’importe où. Une expérience simple, rapide et moderne pour tous les passionnés de lecture.</p>
    </div>
    <div class="right-content">
        <img src="~/images/online-library-illustration.jpg" alt="Illustration" />
    </div>
</div>


<!-- Login Modal -->
<div class="modal fade" id="loginModal" tabindex="-1" aria-labelledby="loginModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content shadow">
            <div class="modal-header border-0">
                <h5 class="modal-title login-title">Connexion</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fermer"></button>
            </div>
            <div class="modal-body">
                @if (ViewBag.Error != null)
                {
                    <div class="alert alert-danger text-center">
                        @ViewBag.Error
                    </div>
                }

                <form method="post" asp-controller="UserAuth" asp-action="Login">
                    <div class="mb-3">
                        <label for="username" class="form-label">Nom d'utilisateur</label>
                        <input type="text" id="username" name="username" class="form-control" required />
                    </div>

                    <div class="mb-3">
                        <label for="password" class="form-label">Mot de passe</label>
                        <input type="password" id="password" name="password" class="form-control" required />
                    </div>

                    <div class="d-grid">
                        <button type="submit" class="login-button">Se connecter</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
